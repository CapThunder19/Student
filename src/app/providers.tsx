'use client';

import { ReactNode } from 'react';

export function Providers({ children }: { children: ReactNode }) {
  // Providers can be added here as needed
  // Currently just a wrapper for future expansion
  return <>{children}</>;
}
